<snippet>
    <content><![CDATA[
@api.constrains('${2}')
def _check_${2}(self):
    for record in self:
        ${0}
        raise UserError(_(u'${1:error message %s}')%(${3:record.${2:field}}))

]]></content>
    <tabTrigger>model/constraints</tabTrigger>
    <scope>source.python</scope>
    <description>Model Python contraints</description>
</snippet>
